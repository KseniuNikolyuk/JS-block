        // Задание 1
        let a = 10;
        alert("Первое значение переменной a: " + a);
        a = 20;
        alert("Второе значение переменной a: " + a);

        // Задание 2
        let yearOfRelease = 2007;
        alert(yearOfRelease);

        // Задание 3
        let name = 'Brendan Eich';
        alert(name);

        // Задание 4    
        let number1 = 10;
        let number2 = 2;
        alert(number1 + number2);
        alert(number1 - number2);
        alert(number1 * number2);
        alert(number1 / number2);

        // Задание 5
        let result = 2 ** 5;
        alert(result);

        // Задание 6
        let share1 = 9;
        let share2 = 2;
        let remainder = share1 % share2;
        alert(remainder);

        // Задание 7
        let quan = 1;
        quan += 5;
        quan -= 3;
        quan *= 7;
        quan /= 3;
        quan++;
        quan--; 
        alert(quan);

        // Задание 8
        let age = prompt("Сколько вам лет?");
        alert(age);

        // Задание 9
        const user = {
            name: 'Dasha',
            age: 17,
            isAdmin: false
        };

        // Задание 10
        let answer = prompt('Как вас зовут?')
        alert(`Привет, ${answer}!`);